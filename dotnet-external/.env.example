# Identity Service — External Client Example (.NET 8)
# Copy this file to .env and replace with your actual credentials from the Admin API.
# External clients use HMAC-SHA256 for token issuance.
#
# .NET loads this .env file at startup and maps values to appsettings.json config.
#
# Usage: cp .env.example .env

CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
SUBJECT=your-subject

# Server configuration
PORT=3501
SIDECAR_URL=https://apidev.abslmfbeta.com/invportal/ipapi/qa/identity-sidecar
