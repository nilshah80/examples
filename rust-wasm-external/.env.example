# Identity Service — Rust WASM Example (Internal Client)
# This file is NOT used by the WASM module itself, but documents the configuration
# that should be set in www/index.html

# Sidecar URL (update in www/index.html CONFIG object)
SIDECAR_URL=http://localhost:8141

# Client credentials (update in www/index.html CONFIG object)
CLIENT_ID=dev-client
CLIENT_SECRET=DevSec-LwgT7vXGZk2njwglKWZBYW7q1sdNTElTQ!

# Subject for token issuance (update in www/index.html CONFIG object)
SUBJECT=test-user

# Note: Unlike backend examples, WASM runs in the browser
# Configuration is set directly in the HTML/JavaScript code
# See www/index.html line ~23 for the CONFIG object
