# Identity Service — External Client Example (Flutter)
# Copy to assets/.env and replace with your actual credentials.
# External clients use HMAC-SHA256 for token issuance.
#
# Usage: cp .env.example assets/.env

CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
SUBJECT=your-subject
SIDECAR_URL=https://your-sidecar-url
